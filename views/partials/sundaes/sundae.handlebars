<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newDevour="{{devoured}}">
		{{#if devoured}}DEVOUR!{{else}}THROW UP!{{/if}}
	</button>

	<button class="delete-sundae" data-id="{{this.id}}">DELETE!</button>
</li>
